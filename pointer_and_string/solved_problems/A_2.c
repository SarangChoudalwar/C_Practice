#include<stdio.h>
int main()
{
    printf(5+"Fascimile\n");
    return 0;
}

/*printf takes the base address adding 5 to the base address points the pointer to 5th offset and hence sould start printing from letter "m" */
